let hey = function hi () {
    console.log('hello'); 
    // the function expression is stored in an expression and can't be hoisted or saved. The variable of 'hey' will be but the expression by itself will not be. //
}